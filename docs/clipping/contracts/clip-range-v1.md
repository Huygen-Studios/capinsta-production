# ClipRangeV1

Range identity is independent of boundaries and order. Transition fields are reserved nullable data only. Canvas supports named ratios or `custom`, and caption tracks reference existing identifiers without duplicating words or renderer state.
