# Production forced-alignment stack for CPU-only VPS deployments.
#
# stable-ts is published as the `stable-ts` package and provides the
# `stable_whisper` Python import used by the backend runtime.
--extra-index-url https://download.pytorch.org/whl/cpu

torch==2.3.1+cpu
torchaudio==2.3.1+cpu
setuptools<82
wheel
more-itertools
numba
tiktoken
openai-whisper==20250625
stable-ts==2.19.1
