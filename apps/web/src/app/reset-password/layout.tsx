import type { Metadata } from "next";
export const metadata: Metadata = { title: "Reset password", alternates: {}, openGraph: null, twitter: null, robots: { index: false, follow: false, nocache: true, noarchive: true } };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
