"use client";
import { useEffect, useState } from "react";
import { Button } from "@/components/ui/button";
type Notification = { id: string; title: string; items: string[]; enabled: boolean; severity: string };
export function SystemStatusManager() { const [items, setItems] = useState<Notification[]>([]); const [title, setTitle] = useState(""); const [details, setDetails] = useState(""); const [startsAt, setStartsAt] = useState(""); const [endsAt, setEndsAt] = useState(""); const refresh = () => fetch("/api/admin/system/status").then(r => r.json()).then(setItems); useEffect(() => { refresh(); }, []); const create = async () => { const r = await fetch("/api/admin/system/status", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ title, items: details.split("\n").filter(Boolean), startsAt: startsAt ? new Date(startsAt).toISOString() : null, endsAt: endsAt ? new Date(endsAt).toISOString() : null }) }); if (r.ok) { setTitle(""); setDetails(""); setStartsAt(""); setEndsAt(""); refresh(); } }; return <div className="space-y-4"><div className="rounded-sm border-2 border-border bg-card p-4"><h2 className="font-black">System status</h2><input value={title} onChange={e => setTitle(e.target.value)} className="mt-3 h-10 w-full rounded-sm border bg-background px-3" placeholder="Status headline" /><textarea value={details} onChange={e => setDetails(e.target.value)} className="mt-2 min-h-20 w-full rounded-sm border bg-background p-3" placeholder="One update per line" /><div className="mt-2 grid gap-2 sm:grid-cols-2"><input aria-label="Starts at" type="datetime-local" value={startsAt} onChange={e => setStartsAt(e.target.value)} className="h-10 rounded-sm border bg-background px-3 text-sm" /><input aria-label="Ends at" type="datetime-local" value={endsAt} onChange={e => setEndsAt(e.target.value)} className="h-10 rounded-sm border bg-background px-3 text-sm" /></div><Button variant="lime" className="mt-3" disabled={!title || !details} onClick={create}>Publish notification</Button></div>{items.map(item => <div key={item.id} className="flex items-center justify-between rounded-sm border p-4"><div><p className="font-bold">{item.title}</p><p className="text-sm text-muted-foreground">{item.enabled ? "Enabled" : "Disabled"}</p></div><Button variant="outline" onClick={async () => { await fetch(`/api/admin/system/status/${item.id}`, { method: "PUT", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ enabled: !item.enabled }) }); refresh(); }}>{item.enabled ? "Disable" : "Enable"}</Button></div>)}</div>; }
