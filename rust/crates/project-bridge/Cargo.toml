[package]
name = "project-bridge"
version = "0.1.0"
edition = "2024"

[dependencies]
clip-domain = { path = "../clip-domain" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
transcript-domain = { path = "../transcript-domain" }

[dev-dependencies]
serde_json = "1"
